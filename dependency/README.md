files under `dependency/`:

- `i3d_torchscript.pt`: checkpoint for Inception3D for FVD calculation
- `weights-inception-2015-12-05-6726825d.pth`: checkpoint for Inception for FID calculation
- `totalseg_v1_label_mapping.txt`: mapping between TotalSegmentator v1 class names and indices
- `totalseg_v1_label_mapping.txt`: mapping between TotalSegmentator v2 class names and indices
- `vgg_lpips.pth`: checkpoint for lpips calculation (perceptual layers)
- `vgg16-397923af.pth`: checkpoint for lpips calculation (VGG16 backbone)